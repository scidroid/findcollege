import styles from "./Confirm.module.css";

const Confirm = () => {
  return (
    <section>
      <h1 className={styles.title}>Check in your email for message from findcollege@scidroid.me</h1>
      <p className={styles.subtitle}>Click the magic link and close this window</p>
    </section>
  );
};

export default Confirm;
